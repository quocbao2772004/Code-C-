#include <bits/stdc++.h>
#define f(i,a,b) for(int i=a;i<=b;i++)
#define fi freopen("PTIT.in","r",stdin)
#define fo freopen("PTIT.out","w",stdout)
#define ll long long
using namespace std;

void goi()
{
    ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    //fi;fo;
}
string s;

int main()
{
    goi();
    getline(cin,s);
    int n=s.length()-1;
     if(s[n]=='y'||s[n]=='Y')
     {
         if(s[n-1]=='p'||s[n-1]=='P')
         {
             if(s[n-2]=='.'){cout<<"yes";return 0;}
         }
     }
    cout<<"no";

    return 0;
}
