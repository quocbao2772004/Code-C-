#include<bits/stdc++.h>
#define ll long long
#define f(i,a,b) for(int i=a;i<=b;i++)
#define f1(i,a,b) for(int i=a;i>=b;i--)
using namespace std;
void goi()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
}
string s;
int main()
{
    goi();
  
        cin>>s;
       f1(i,s.length()-1,0)cout<<s[i];
    return 0;
}
