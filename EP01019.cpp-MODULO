#include<bits/stdc++.h>
#define ll long long
using namespace std;
void goi()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
}
ll a[5];ll e;
int main()
{
    goi();
    for(int i=1;i<=4;i++)cin>>a[i];
    sort(a+1,a+4+1);
    ll x=__gcd(a[2]-a[1],a[3]-a[2]);
    ll y=__gcd(a[4]-a[3],a[3]-a[2]);
    e=__gcd(x,y);
    cout<<e;

    return 0;
}
