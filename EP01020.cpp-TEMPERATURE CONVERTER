#include<bits/stdc++.h>
#define ll long long
using namespace std;
void goi()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
}
double n;
int main()
{
    goi();
    cin>>n;
    cout<<fixed<<setprecision(2)<<(n*9/5)+32;

    return 0;
}
